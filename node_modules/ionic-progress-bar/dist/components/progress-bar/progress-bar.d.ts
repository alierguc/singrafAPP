import { OnInit, OnDestroy, ChangeDetectorRef } from '@angular/core';
import { NavParams, Events } from 'ionic-angular';
export declare class SimpleProgressBarComponent implements OnInit, OnDestroy {
    params: NavParams;
    events: Events;
    private ref;
    message: string;
    progress: number;
    protected _total: number;
    protected _actual: number;
    constructor(params: NavParams, events: Events, ref: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    _milestone(params: any): void;
}
