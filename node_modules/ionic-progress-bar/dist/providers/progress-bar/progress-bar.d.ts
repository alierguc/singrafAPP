import { ModalController, Modal, Events } from 'ionic-angular';
import { Observable } from 'rxjs/Rx';
export declare class SimpleProgressBarProvider {
    modalCtrl: ModalController;
    eventCtrl: Events;
    protected _profile_modal: Modal;
    constructor(modalCtrl: ModalController, eventCtrl: Events);
    present(message: string, milestones: number): Observable<void>;
    setMilestone(message: string): void;
    dismiss(): void;
}
